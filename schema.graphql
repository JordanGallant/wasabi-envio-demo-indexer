type WasabiOption_Approval {
  id: ID!
  owner: String!
  approved: String!
  tokenId: BigInt!
}

type WasabiOption_ApprovalForAll {
  id: ID!
  owner: String!
  operator: String!
  approved: Boolean!
}

type WasabiOption_OwnershipTransferred {
  id: ID!
  previousOwner: String!
  newOwner: String!
}

type WasabiOption_Transfer {
  id: ID!
  from: String!
  to: String!
  tokenId: BigInt!
}

